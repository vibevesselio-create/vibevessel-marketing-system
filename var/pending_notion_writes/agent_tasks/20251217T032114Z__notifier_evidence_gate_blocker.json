{
  "title": "Unblock Notifier Evidence Gate for smoke-run-006 (Slack + macOS)",
  "priority": "High",
  "status": "Not Started",
  "dependency_status": "Blocked",
  "assigned_agents": ["Codex MM1"],
  "review_handoff_to": ["Claude MM1", "ChatGPT Strategic Agent"],
  "objective": "Resolve blockers preventing smoke-run-006 evidence bundle: provide Slack webhook env to LaunchAgent runtime and restore macOS notification path (osascript or terminal-notifier).",
  "preflight": [
    "Confirm webhook server runs as LaunchAgent in GUI/user domain.",
    "Verify env precedence: env vars override .env; .env fills only missing values.",
    "Ensure SLACK_WEBHOOK_URL is injected via LaunchAgent plist EnvironmentVariables (no secrets logged)."
  ],
  "steps": [
    "Inject SLACK_WEBHOOK_URL into ~/Library/LaunchAgents/com.serenmedia.webhook-server.plist (EnvironmentVariables) and restart LaunchAgent; do not log the URL.",
    "Retest macOS notification path: /usr/bin/osascript -e 'display notification "Test body" with title "Webhook Server"' in LaunchAgent context; if still failing -2740, install user-local terminal-notifier and use that fallback.",
    "Re-run smoke-run-006 real endpoint; capture webhook log receipt→processing→completion for run_id, dashboard /dashboard/api/status OK, widget screenshot, macOS banner, Slack message (redacted).",
    "Package evidence under canonical log root with NOTES.md (notifier method used + why, Slack HTTP status, redaction)."
  ],
  "success_criteria": [
    "Slack notifier logs presence=True and HTTP 2xx for smoke-run-006 (URL not logged).",
    "macOS notifier logs method={osascript|terminal-notifier} rc=0 and banner visible for run_id.",
    "Evidence bundle includes log snippet, dashboard JSON excerpt, widget screenshot, macOS notification screenshot, Slack screenshot (redacted) for smoke-run-006.",
    "No secrets in logs/screenshots; guarded-success language only after checklist satisfied."
  ],
  "created_at": "20251217T032114Z",
  "metadata": {
    "run_id": "smoke-run-006",
    "slack_webhook_present": false,
    "osascript_error": "-2740",
    "terminal_notifier_installed": false
  }
}
