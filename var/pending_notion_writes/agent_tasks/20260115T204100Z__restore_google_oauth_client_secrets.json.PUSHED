{
  "title": "Restore Google OAuth client secrets file or update env path",
  "priority": "High",
  "status": "Not Started",
  "dependency_status": "Ready",
  "assigned_agents": ["Codex MM1"],
  "review_handoff_to": ["Claude MM1"],
  "objective": "Ensure the Google OAuth client secrets JSON exists at the configured path so /auth/google can initialize.",
  "preflight": [
    "Identify the intended Google OAuth client (Web application)"
  ],
  "steps": [
    "Locate the latest client_secret JSON file for the VibeVessel OAuth client",
    "Copy it to the configured path or update GOOGLE_OAUTH_CLIENT_SECRETS_FILE to the correct path",
    "Restart webhook server and confirm Google OAuth handler initializes"
  ],
  "success_criteria": [
    "Webhook server logs show Google OAuth handler initialized",
    "GET /auth/google returns an authorization URL"
  ],
  "created_at": "20260115T204100Z"
}
